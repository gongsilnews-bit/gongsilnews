const fs = require('fs'); const env = fs.readFileSync('.env.local', 'utf8'); const url = env.match(/NEXT_PUBLIC_SUPABASE_URL=(.*)/)[1]; const key = env.match(/SUPABASE_SERVICE_ROLE_KEY=(.*)/)?.[1] || env.match(/NEXT_PUBLIC_SUPABASE_ANON_KEY=(.*)/)[1]; const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(url, key); supabase.auth.admin.listUsers().then(res => console.log(JSON.stringify(res.data.users.filter(u => u.email === 'gongsilmarketing@gmail.com').map(u => ({id: u.id, email: u.email, providers: u.app_metadata.providers})), null, 2)));
